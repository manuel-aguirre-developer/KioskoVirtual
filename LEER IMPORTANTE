cosas nuevas a tener en cuenta:
--------------------
- alter table ventas add column abono decimal(10,2);
- revisar la estetica de la vista de mispedidos
---------------------

# Comandos básicos y esenciales para Git

# 1. Inicializar un repositorio Git local
git init

# 2. Clonar un repositorio remoto
git clone <url-del-repositorio>

# 3. Ver el estado de los archivos (modificados, no rastreados, etc.)
git status

# 4. Agregar archivos al área de staging
git add <archivo>         # agregar archivo específico
git add .                 # agregar todos los cambios

# 5. Crear un commit con mensaje
git commit -m "Mensaje descriptivo del commit"

# 6. Ver el historial de commits
git log

# 7. Ver las ramas existentes y en cuál estás
git branch

# 8. Crear una nueva rama
git branch <nombre-rama>

# 9. Cambiar de rama
git checkout <nombre-rama>

# 10. Fusionar una rama en la actual
git merge <nombre-rama>

# 11. Subir cambios al repositorio remoto
git push origin <rama>

# 12. Traer cambios del remoto y actualizar la rama local
git pull origin <rama>

# 13. Revertir cambios en un archivo (antes de commitear)
git checkout -- <archivo>

# 14. Mostrar diferencias en archivos modificados
git diff

# 15. Eliminar un archivo y hacer commit del cambio
git rm <archivo>
git commit -m "Eliminar archivo <archivo>"

# 16. Configurar usuario (nombre y email)
git config --global user.name "Tu Nombre"
git config --global user.email "tu.email@ejemplo.com"

# 17. Mostrar configuración actual de Git
git config --list

# 18. Mostrar diferencias entre ramas
git diff <rama1> <rama2>

# 19. Rebase para integrar cambios de otra rama
git rebase <rama-base>

# 20. Ver resumen gráfico y compacto del historial de commits
git log --oneline --graph --all

